events {
  worker_connections 1024;
}

http {

  server {
    listen 192.168.56.60:{{ nginx_listen_port }};
    
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    location / {
      root /usr/share/iso/;
      autoindex on;  
    }

  }

}

