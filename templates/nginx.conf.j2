events {
  worker_connections 1024;
}

http {

  server {
    listen 10.0.0.20:{{ nginx_listen_port }};
    
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    location / {
      root /usr/share/iso/;
      autoindex on;  
    }

  }

}

